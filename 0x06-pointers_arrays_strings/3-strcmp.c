#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 * _strcmp - - it compares to strings
 * @s1:paremeter
 * @s2:parameter
 * Return:0
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
	return (0);
}
