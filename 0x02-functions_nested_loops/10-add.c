#include "main.h"
/**
 * add - add two integers and return the sum
 * Return :Always 0 success
 */
int add(int b, int a)
{
	int sum ;

	sum = a + b;
	_putchar(sum + '0');
	return (sum);
return (0);
}
