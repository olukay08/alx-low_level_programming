#include "main.h"
/**
 * add - add two integers and return the sum
 * Return: Always 0 success
 * @b:argument for the function
 * @a :second argument for the function
 */
int add(int b, int a)
{
	int sum;

	sum = a + b;
	return (sum);
return (0);
}
